[variables]
managed_worker_secret = "${base64:32}"
docker_registry_password = "${base64:24}"

[config.env]
# ============================================================================
# Core
# ============================================================================
TRIGGER_IMAGE_TAG = "v4-beta"

# ============================================================================
# Image Tags (leave empty to use defaults)
# ============================================================================
DOCKER_PROXY_IMAGE_TAG = ""

# ============================================================================
# Secrets (must match triggerdevv4 Webapp blueprint)
# ============================================================================
MANAGED_WORKER_SECRET = "${managed_worker_secret}"

# ============================================================================
# API Connection (internal)
# ============================================================================
TRIGGER_API_URL = "http://webapp:3000"
OTEL_EXPORTER_OTLP_ENDPOINT = "http://webapp:3000/otel"

# ============================================================================
# Docker Registry (must match triggerdevv4 Webapp blueprint)
# ============================================================================
DOCKER_REGISTRY_URL = "registry:5000"
DOCKER_REGISTRY_USERNAME = "trigger-registry"
DOCKER_REGISTRY_PASSWORD = "${docker_registry_password}"
DOCKER_REGISTRY_NAMESPACE = "trigger"

# ============================================================================
# Restart & Logging
# ============================================================================
RESTART_POLICY = "unless-stopped"
LOGGING_DRIVER = "local"
LOGGING_MAX_SIZE = "20m"
LOGGING_MAX_FILES = "5"
LOGGING_COMPRESS = "true"
